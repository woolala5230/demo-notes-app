import { FunctionState, State } from "./router";
import { EventDelegate } from "../events";
import { WritableDraft } from "immer/dist/internal";
declare type Opts = {
    port: number;
    region: string;
    app: string;
    stage: string;
    live: boolean;
};
export declare function useLocalServer(opts: Opts): {
    port: number;
    updateState: (cb: (draft: WritableDraft<State>) => void) => void;
    onDeploy: EventDelegate<void>;
    updateFunction(id: string, cb: (draft: WritableDraft<FunctionState>) => void): void;
};
export {};
