import { Config } from "../config";
declare type Events = {
    type: "FILE_CHANGE";
} | {
    type: "TRIGGER_DEPLOY";
} | {
    type: "BUILD_SUCCESS";
};
declare type Context = {
    dirty: boolean;
    deployedHash: string;
    pendingHash: string;
};
export declare function useStacksBuilder(root: string, config: Config, cdkOptions: any, deployFunc: any): import("xstate").Interpreter<Context, any, Events, {
    value: any;
    context: Context;
}>;
export {};
